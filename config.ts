// Module that contains all the configuration for the bot

// The setting to enable setting the webhook
// Should be turned off after setting the webhook in production
export const ENABLE_SETTING_WEBHOOK = false;

// The setting to enable the check for admins in group chats
// for the poll and training message commands
export const ENABLE_ADMIN_CHECK = false;

// The setting to enable deleting command messages in group chats
// for the poll and training message commands
export const ENABLE_DELETING_COMMAND_MESSAGES = true;
